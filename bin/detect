#!/bin/bash

# Check if file exists
if [ ! -f "$3/ENFORCE_APP_NAMING_PATTERN" ]; then
    echo "ENFORCE_APP_NAMING_PATTERN is set. App name must start with $ENFORCE_APP_NAMING_PATTERN"
    exit 0
fi

exit 1
